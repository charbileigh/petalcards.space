{
  "name": "petalcards",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "A private, self-hosted flashcard app with spaced review.",
  "engines": {
    "node": ">=24.0.0"
  },
  "scripts": {
    "start": "node src/server.mjs",
    "dev": "node --watch src/server.mjs",
    "test": "node --test",
    "check": "node --check src/server.mjs && node --check src/database.mjs && node --check src/security.mjs && node --check public/app.js"
  },
  "license": "MIT"
}
